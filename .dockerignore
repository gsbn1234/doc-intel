# 构建上下文排除项：这些文件不发给 Docker 守护进程。
# 最关键：.venv 5GB+，不排除每次 build 都往 daemon 传 5GB
.venv
__pycache__
*.pyc
.git
.idea
.env
.pytest_cache
faiss_db
# 已作废（对话记忆改存 MySQL，compose 也不再挂载它），但这个 0 字节的旧文件还留在
# 工作目录里，留着这行是为了别让它被 COPY 进镜像
checkpoints.sqlite
docs
tests
app
*.md
*.pdf
